# AGENTS.md — undertow

期权/持仓情报工具，服务于一个**真实的小额实盘账户**（净资产约 $440）。
纯标准库 Python、零依赖、确定性计算。本文件是给在本仓库工作的 AI 代理
（Codex / Claude Code / 其它）的统一上手说明。

> 面向终端用户的技能说明（何时用哪个命令、如何解读输出）在 **`SKILL.md`**；
> 本文件只讲**在这个仓库里写代码时必须知道的事**，不重复 SKILL.md 的内容。

---

## 一、不可协商的四条边界

违反其中任何一条的改动都不该提交，即使用户明确要求。

1. **只读，绝不交易。** 任何代码路径都不得下单、撤单、改单，包括条件单/止损单。
   长桥 CLI 只用 `quote / depth / positions / assets / cash-flow / order`（只读子命令）
   与 `order detail|executions`。**`order buy|sell|cancel|replace` 永远不出现在本仓库。**
   执行永远是用户在券商端的动作。
2. **零第三方依赖。** 可视化是手写 SVG，不用 matplotlib；HTTP 用 `urllib`；
   统计用 `statistics`/`math`。这是项目身份，不是偏好。
3. **不绕过任何数据源的反爬/ToS。** CME 403、ForexFactory 网页 Cloudflare 一律不碰；
   只用可合法访问的 CFTC / CBOE / Yahoo / FRED / FairEconomy 公开 JSON feed。
4. **敏感数据永不入库。** 已 gitignore：`data/account/`（实盘持仓资金）、
   `data/soul/`（个人交易体系、亏损史、心理弱点）、`article`、`docs/screenshot/`、
   `docs/author_*.md`（付费订阅内容，含水印，涉版权）。
   **每次提交前跑一遍**：
   `git status --short | grep -iE "article|screenshot|author|playbook|private|account/|soul/"`

---

## 二、跑起来

```bash
python3 -m pytest -q                 # 应全绿（当前 272 项）
python3 -m undertow list             # 品种清单
python3 -m undertow report gold      # 综合研判 → data/reports/*.html
python3 -m undertow live             # 持仓实时体检（需长桥 CLI）
```

品种：`gold silver wti dxy qqq tqqq tlt spy iwm`
  交易 `gold silver qqq tqqq` ／ 分析 `wti dxy tlt spy` ／ 观察 `iwm`（为将来攒链，非独立信息源）
命令：`analyze gamma vol snapshot flow expiry fib backtest report list account
consult serve soul journal event plan tech live backtest-stretch news calendar`

---

## 三、代码地图（严格单向依赖，改一层不动其它）

```
core/      models / config / clock / calendar        —— 不依赖任何其它层
collect/   数据源适配器 + 快照仓库 + 缓存             —— 取数只在这层
analyze/   纯计算（只吃 core.models，无 I/O、无网络） —— 数值全出自这层
report/    markdown / html / viz（只吃 analyze 结果）
consult/   AI 接入层：上下文包 + 本地只读 HTTP
soul/      用户的交易体系 / 计划 / 日记（数据私有，代码公开）
cli.py     命令编排（唯一允许把 collect 与 analyze 接起来的地方）
```

各层另有 `README.md` 讲清每个文件的职责与已知边界，**改动请同步更新**。
`config/instruments.json` 是品种注册表——加品种改 JSON，分析层不动。

---

## 四、这个项目特有的工程约定

这些不是通用最佳实践，是在这个仓库里踩出来的。

### 数值与统计

- **LLM 不碰算术。** 所有数字由 `analyze/` 的确定性代码算出；新增计算必配单测。
- **任何阈值都要能被证伪。** 拍脑袋的阈值不许进代码。若一定要拍，
  必须在注释里标明「未校准」，并提供重跑校准的命令
  （参考 `stretch.py` 的 `CALIB` + `backtest-stretch --emit`）。
- **校准数字只能有一处来源。** 散文注释里写死样本量必然过期
  （面板每天增长）。用 `CALIB_META` 这类结构集中存放，文案去读它。
- **回测三条硬要求**（见 `stretch_backtest.py` 文档）：
  局部去趋势（不减全样本均值）、同 regime 内比较、不重叠子样本 + 双样本 t
  （边缘与 t 必须同源）。
- **显著性要同时看 t 与检验样本量。** 只看 t 会把 n=36 的格子当成证据。
- **已知的统计局限必须与结论同时呈现**，不能只在文档角落提一句。
- **回测不了的东西，先建台账再说。** 有些层拿不到历史数据（期权链只有当前快照，
  免费源没有历史逐行 OI），此时不许用"讲得通的故事"去调阈值——2026-08-27 差点
  因此把全集最准的信号掐掉。正确做法：把每次触发的**原始分量**落盘、**连被闸门
  拦下的候选一起记**（否则永远没有反事实样本），事后回填真实走势，攒够 n 再说。
  见 `signal_ledger.py` 与 `undertow signals --rebuild --backfill`。
- **未校准的层不得拥有比已校准的层更强的视觉权重，更不得反转它的结论。**
  强信号是全报告唯一顶红色 ⚡ 横幅、却从未回测过的一层；超买超卖有 33 年校准
  反而只是一行标签。原则：未校准层只能【加持同向】读数，不能【反转反向】读数。
  旧版本里看跌强信号是 `verdict` 第一个分支，直接盖掉「中期偏多、趋势未坏」，
  把总纲翻成「可空」——一个没有任何统计证据的单层规则否决了综合研判。
- **时序约定（写错就是前视）：** 快照文件日期 D 在 D 当天盘前抓取，其 OI 是
  OCC 在 **D−1 收盘后**隔夜结算的 → `diff(D−1, D)` 描述的是 **D−1 那个交易日**
  的持仓变化，而它在 **D 开盘前**可读、可用于交易 **D 当天**。
  复盘打分只能是「(X, X+1) 描述交易日 X，其可兑现结果是交易日 X+1 的走势」。
  详见 `flow.py` 顶部的时序约定块。
- **不要把外部作者当成真值，价格才是。** 2026-08-27 复盘实测：黄金 6 个关键节点里，
  某外部分析在 4 次判防守/看跌而实际都在涨；我们的「偏多」反而对。
  当时我正准备继续"修"其中一个节点去对齐他的结论 —— 那会把一个正确判断改错。
  对标外部方法可以借鉴**指标构造**，但**对错只能由实际走势裁定**。
- **前瞻收益的基准必须是信号发出【之前】就已知的价格。** 快照文件名日期 D 实际是
  ET 01:00~02:00 抓的，装 D-1 结算的 OI，报告在 D 开盘前可读 → 信号在 D 开盘可执行。
  拿 `close[D]` 定基既漏掉 D 当天收益、又是前视。取 D 之前最后一个已知收盘，
  并把 `base_date` 存盘可审计。
- **未知就是未知。** 事后重放拿不到当时的综合研判（COT/宏观会被修订），
  此类字段必须存 `None`，不能落下某个恒真的默认值——旧台账 31 行全部
  `outlook_bias=""` 却有 14 行 `diverges=true`，就是这么来的。
- **损坏的数据文件必须隔离保留，绝不能被下一次写入静默抹掉。** 历史不可再生。

### 静默失败（2026-08-28 一天内在八处发现同一模式，逐条列出以免重犯）

**判据：任何"本该发生却没发生"的事，用户必须当场知道，不能只写进日志。**
整条交易流程依赖每日研报（据其期权结构决定交易策略），缺一天必须有人知道。

1. **失败只写日志 = 没人知道。** 8/21、8/22 两次全部品种网络失败、
   "没有保存任何快照"，只写进 `~/Library/Logs/`，用户十天后才发现。
   → 失败必须**推送 + 落兜底文件**（`data/reports/FAILURE_<date>.txt`，纳入 git）。
   只弹通知不够：launchd 下 osascript 未必弹得出。
2. **判成败不得 grep 人读文案。** 改一句提示文案，告警就静默失效。
   → 用 `--status-file` 输出带 `schema` 的原子 JSON，人读输出保持不变。
   状态需分 `complete / partial / failed / unchanged / crashed` —— 其中
   **`crashed`（状态文件缺失或损坏）必须与"跑完但失败"区分开**。
3. **正常的"没数据"不得告警。** OCC 隔夜结算时刻本有波动，早时点抓不到是
   设计内行为（`unchanged`）。若也告警就成了每天狼来了，真出事反被忽略。
4. **时刻判据不得写死目标时区的钟点。** plist 时点是【本地时间】，
   ET 随夏令时漂 1 小时 —— 写死 "ET_MIN>=08:30" 会让**冬令时半年内永不触发**。
   → 从 plist 读本地时点比较，与时区无关。
5. **`$(cmd) || true` 会把退出码永远变成 0**（实测），进程崩溃被当成正常跑完。
   → `set +e` 分开捕获 rc。
6. **shell 函数必须定义在调用之前**，否则真出错时是 command not found。
   → 测试断言"定义行号 < 所有调用行号"。
7. **落盘必须原子 + 回读验证；损坏必须隔离。** 半截坏文件 + load 静默返回 None
   + 上层"文件存在即算齐全" = 不可再生的数据被静默覆盖。
8. **`git status --porcelain` 不能当本次运行结果** —— 它会把运行前就有的脏文件
   当成本次新增，在什么都没抓到的日子照样出报告并提交。
9. **调度层也会静默失败，而且更难发现。** `session_hooks.sh` 成功时写 md、
   跳过时什么都不留，于是「launchd 没唤醒」与「唤醒了但判断为不该跑」
   在事后完全无法区分 —— 用户问「时点生效了吗」时我答不上来，
   只能靠 `runs` 计数猜。**每个无人值守脚本每次唤醒都必须留痕**：
   窗口内写日志行（记决策，不记账户内容），窗口外只 touch 心跳文件的 mtime。

### launchd：别用密集的 StartCalendarInterval（2026-08-28 实测）

session 任务原先挂 15 个日历时点（EDT/EST 各一套 × 三个窗口）。
**当天该响 12 枪，实际几乎一枪没投** —— ET 09:42 的持仓体检、ET 10:12 的
事件后复核全部没跑。已逐一排除：环境（`env -i` 模拟精简环境跑，`live` 照样成功，
凭证走文件不走环境变量）、plist 未重载（磁盘与 launchd 内存里的 15 个时点逐一
比对完全一致）、机器睡眠（同期 eventwatch 正常提交）。

同一台机器上用 `StartInterval` 的任务从不出错：eventwatch(600s) 353 次、
spreadlog(300s) 587 次。**改用 `StartInterval=300` 轮询，脚本自己按真 ET 判窗口。**
附带好处：夏令时不用再在 plist 里挂两套时点，脚本读真 ET 自动适应。

判据：**需要「在某个时间窗内至少跑一次」时用 StartInterval + 脚本判窗口；
StartCalendarInterval 只留给条目少、且错过一次无所谓的任务**（daily 4 个时点正常）。

**编辑代码时同理**：字符串替换必须 `assert` 匹配成功。本轮两处 `replace` 未加断言、
静默没改上，直到冒烟测试全不触发才发现 —— 修静默失败的过程本身又静默失败了一次。

**告警必须能冒烟测试**：每个状态分支都要能人工构造并验证触发（本轮四态逐一实测过）。

### 期权流：三条被市场纠正过的判读规则（2026-08-28 黄金/白银）

当天 8/28 GLD **−3.24%**（收 408.89）、SLV **−4.38%**，是一波贵金属抛售。
研报里黄金标 ⚡极强看跌（对），白银标偏多（错）。复盘出三条：

1. **短到期 + 低 Δ ≠ 彩票，恰恰是最强的定向押注。**
   黄金 413P/408P：次日到期、Δ0.10/Δ0.04、共 7.8 万张、成交≈ΔOI（新开仓非换手）、
   纯净度 0.97/0.89、IV 被买涨 +3.63pp。次日正好打穿 408。
   我当时按「|Δ|<0.10 → 彩票」贴了标签，**把最强的信号贴成了最弱的**。
   正确判据：长到期+低 Δ = 真尾部险（方向信息弱）；
   **短到期(≤2天)+低 Δ+大量新开仓 = 只有立刻大跌/大涨才值钱（方向信息最强）**。

2. **call/put 张数比会误导，必须按买卖方分侧。**
   白银 index 写「call 是 put 的 2.2 倍」看着像看涨，可那批 call（69C/67C，
   纯净度 0.95+）全是**卖方压制**——卖上方 call 是看跌动作。
   按买卖方分侧后是「看跌 2.4 倍」，与当天 −4.38% 一致。
   **看跌侧＝买 put ＋ 卖 call；看涨侧＝买 call ＋ 卖 put。**

3. **持仓流与综合结论相反时要自己说出来，不能等读者撞上。**
   黄金/白银当天都是「综合偏多 + 持仓流看跌」，页面上两个数字并排却不点破。
   用户的核心抱怨就是「指标之间互相打架」——藏起来不会让矛盾消失。

**一次命中不等于信号有效。** 同一天八品种按幅度≥0.3% 计只有 3/6 对，
而且黄金白银是同一波行情却给出相反读数——**如果信号真在读聪明钱定位，
两个相关 0.89 的品种不该背向**。黄金这一枪打中，更可能是那批 0DTE put
确实是知情押注，而非整套框架有效。结论仍以台账样本为准，不以单次结果为准。

### 周末拿不到周五的 OI（2026-08-30 实测）

**周五收盘的 OI 结算，周六和周日都拿不到，要等周一凌晨。**两个周末一致：

| | Σ\|ΔOI\|（存活合约） |
|---|---|
| 8/21(五) → 8/22(六) | **0** |
| 8/22(六) → 8/23(日) | **0** |
| 8/23(日) → 8/24(一) | 892,751 ← 周一 ET 04:09 才到 |

8/28 那一周完全相同：周六守到 ET 07:42、周日 ET 00:37 均为 0。
→ **「周一早上的研报」描述的是【上周五】的交易日，中间隔了两天。**

⚠️ 判断「结算到了没有」**只能用 `oi_change_total()`（存活合约的 Σ|ΔOI|）**，
不能用 OI 总量之差 —— 到期合约滚出会让总量变化，看着像有新数据。
我在 2026-08-29 用总量差误判过一次「周六拿到了周五数据」，
隔天复盘讲历史规律时**又用同一个错口径重复了一遍**。
同一个坑栽两次，是因为第一次只修了代码、没把判据写进文档。

### 单品种时序 vs 跨品种合并：两把不同的尺子（2026-08-29）

我在一天里在这上面栽了两次，方向相反：

**第一次（过松）**：拿 138 个「品种日」直接做 Wilson 检验，宣布增仓层已验证。
错在跨品种同日高度相关（金银 0.89、QQQ/TQQQ 0.99），138 个品种日不是
138 份独立证据。codex 指出后改用日聚类 bootstrap —— 只有 35 个聚类 <50，撤回。

**第二次（过紧）**：撤回时连单品种结论一起撤了。用户质疑「增仓在黄金几个
大涨大跌节点都有效，这是事实吧」——是事实，而我错在用一把为跨品种设计的
尺子去量单品种。

**判据**：
- **跨品种合并** → 必须日期聚类 bootstrap。同一天的样本不独立。
- **单品种时序** → 日聚类修正【不适用】。同品种不同日、前瞻 1 日、
  收益窗口不重叠，独立性假设成立，二项检验就是对的工具。

实测（增仓层方向 vs 当日去趋势收益）：

| 品种 | 样本 | 命中率 | 二项双尾 p | Wilson 下界 |
|---|---|---|---|---|
| 黄金 | 32 | 72% | **0.0201** | **54.6%** |
| 白银 | 28 | 71% | **0.0357** | **52.9%** |
| 原油 | 30 | 53% | 0.856 | 36.1% |
| QQQ | 12 | 58% | 0.774 | 32.0% |

黄金大波动日单独看 **9/11 = 82%**，含 8/19(+3.84%) 与 8/28(−3.24%)。

**但仍够不上「已验证」，三条都差着**：
1. Bonferroni 校正（测了 4 个品种）后阈值 0.0125，黄金 p=0.020 过不了；
2. n=32 < 项目自己定的 MIN_N=50；
3. 只覆盖两个月、一种市场环境。原油 53%、QQQ 58% 说明它不放之四海皆准。

**定性：比「探索性观察」强，够不上「已验证」。可用于加权判断，不足以据此重仓。**

⚠️ 撤回结论时要和下结论时一样小心 —— 过度撤回同样是不诚实，
它会把真实存在的信号一起否认掉。

### 回测必须按行情分层（2026-08-29，用户提出）

「横盘的日期和大涨大跌的日期要区分。横盘的时候，指标乱是正常的。」
这个改进直接救活了两个此前被误判为无效的指标：

| 指标 | 混在一起 | 大波动日 | 横盘日 |
|---|---|---|---|
| 💰 增仓 | 64.3% | 63.9% [50.0,77.4] | 64.5% [53.9,74.6] |
| 🌊 波动 | **52.1%（判为无效）** | **72.7% [55.2,88.6]** | **41.3%（反向）** |
| 📈 价格 | 60.2%（贴边） | **73.5% [57.1,87.9]** | 53.7% |

波动率面混在一起测只有 52.1%，等于把 72.7% 和 41.3% 两个**方向相反**的东西
平均掉了。**不分层的回测会把「只在特定行情下有效」误判成「无效」。**

配套规矩：
- **大波动门槛按各品种自身 |日涨跌| 的 70 分位定**，不用统一的 1.5%
  —— 原油日常波动本就比黄金大，一刀切会把原油的横盘算成大波动。
- **按当日实际涨跌分层用的是事后信息。** 结论只能说「指标在大波动日更准」
  （描述性），不能直接说「只在大波动日用它」（可执行）——事前并不知道
  哪天会大波动。两者之间隔着一个必须单独验证的预判环节。
- 实测：前日波动大 → 今日为大波动日的比例 23%，前日波动小 → 45%，**负相关**。
  波动不聚集，不能用前日波动择时。
- 🔴 事件日增仓命中 82%（n=24）vs 其余日 60% —— 方向可期但样本远远不够。

### 数据口径

- **加品种的判据是【持仓/结构层是否独立】，不是价格相关性。**
  SPY 与 QQQ 日收益相关 0.95 却仍值得加——因为投机资金持仓的周变化相关只有 −0.07。
  反过来，价格不相关但持仓层缺失（如股票板块 ETF 无 CFTC COT）的品种不该加。
- **ETF 代理 vs 商品价**：行权价以 ETF 计，换算商品价用**当日实时比值**
  （`期货价 / ETF价`），不用静态乘数——乘数随基金费率漂移。
  位点必须换算；而 `(价差)/(ATR)` 这类**比值**量纲自动抵消，不用换算。
- **盘中数据源分工**：长桥实时（`quote` 给价与 IV、`depth` 给 Level-2 盘口），
  CBOE 延迟 15 分钟只用于隔夜结构分析（OI 墙来自 OCC 隔夜结算，盘中恒定不变）。
- **券商「成本价」在部分减仓后会被改写**成该轮打平价，既非实付价也不含更早轮次。
  判断盈亏一律用现金流水（见 `livecheck.Ledger`）。
- **出场计价**：多头腿按 bid、空头腿按 ask。券商 App 的持仓盈亏用 last，
  对流动性差的腿会系统性高估。

### 失败与缺失

- **静默失败是最严重的 bug 类别。** 「检查失败」与「没事发生」必须可区分。
  无人值守脚本尤其：先写临时文件、逐条检查退出码、全部成功才原子改名；
  失败要能被下一次唤醒重试，不能留下一个「成功」文件把当天堵死。
- **缺数据就返回 `None`，不要折成 0。** 折零会把「拿不到行情」显示成
  「持仓已归零」。聚合时若有成员缺失，总额必须显式标为「不完整」——
  **低估敞口比高估危险**。
- **不要用 `if x:` 判断数值缺失。** `0.0` 是合法值，而且往往正是最需要告警的时刻。
  一律 `x is not None`。

### 重复实现

- 同一个量不许在两处各算一遍。已有 `tests/test_no_drift.py` 守住
  `_yearfrac` / `_atr` / `_sma` 的跨模块一致性——新增共用计算请一并加进去。
- 「详细结论」与「摘要标签」必须在**同一个分支**里产出。两套并行 if 链必然漂移
  （`verdict.py` 曾漂出三处自相矛盾）。

---

## 五、金融语义（改 `analyze/` 前必读）

- **买方与卖方是两套框架，不可混用。**
  买方（付权金）：看**净Δ**与**每日 theta 打平所需波动**，因为要提前平仓；
  卖方（收权金）：才看**到期越过行权价的概率**。
  用错框架会选出完全错误的结构。注意净Δ判闸门要用 `abs()`——
  看跌结构的净Δ本就为负。
- **借记价差的最大亏损 = 已付权金**，价值下界为 0、跌不穿，没有强平尾巴。
  这类结构的止损是「减损选择」，不是「防灾必需」。
- **仓位双层限额**：单笔止损风险 ≤10%（定仓位）、单笔最大亏损 ≤20%（防跳空）。
- **手续费按张计**，不按合约代码数。
- **组合单 vs 分腿建仓决定出场机制**：组合单进 → 券商锁腿、只能整体平；
  分腿进 → 不认作组合、必须逐腿手动且**永远先买回短腿**（否则留下裸卖）。

---

## 六、诚实边界（改动与汇报时都要保持）

- 期权层是 **ETF 代理**（USO ≈ WTI 属弱代理，无稳定乘数）；
  真·CME 期货期权需券商/付费源，尚未接入。
- COT 周频、发布滞后约 3 天。
- 输出只作**波段级风险情景**，不是交易指令、不是投资建议。
- 不确定就说不确定。**推测不能当作事实写进代码注释或规则**——
  若要断言券商/交易所的行为，先去查；查不到就明说查不到。

---

## 七、提交

- 提交信息讲清**为什么**，不只是改了什么；bug 修复要写明**原来错在哪、后果是什么**。
- 快照（`data/snapshots/`）纳入 git——期权链不可再生，入库即备份。
- 提交前：`pytest -q` 全绿 + 敏感文件检查（见第一节）。
