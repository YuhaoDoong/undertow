# 2026-08-28 → 08-29 更新整理

起点是一句抱怨：「index 里全是模板化，不做空长线拿住，都没有意义」。
终点是一套按验证结果分权的指标体系，中间修掉 6 个会误导决策的缺陷，
其中 3 个是我自己在这轮里引入的。

---

## 一、修掉的缺陷（按严重度）

### 🔴 P0-1 净有效 Delta 在短到期主导时把强信号打成低置信
`undertow/analyze/direction.py`

8/28 黄金：推断口径 **53.49×** 指向偏空，净 Delta **+2,993** 指向偏多 →
两口径反向 → `low_confidence` → 我在对话里没当回事、没预警。
次日 GLD **−3.24%**，收 408.89，正好打穿那批被忽略的 408P。

**根因**：那 7.8 万张 put 是次日到期、Δ0.04/0.10，按 Delta 加权几乎不计分；
call 端只有 35,235 张却带 Δ0.50，全额计分。
**Delta 是局部线性近似，对"到期前大幅跳跃"系统性失效** ——
0DTE 的 408P 当时 Δ0.04，真跌 3% 就变 Δ1.0，赔率几十倍。

**修法**：`dte_share_le2 ≥ 0.5` 时净 Delta 失去反证资格，只作提示保留。
阈值取 0.5（过半）而非拟合值 —— 没有样本可校准，只能取不偏不倚的分界。

### 🔴 P0-2 回放吃到未来价格（本轮自己引入）
`undertow/cli.py`

`--as-of` 回放 8/28 时超买超卖显示「中性 57%」，而当天真实报告是「偏超买 78%」。
**回放吃进了 8/28 当天 −3.24% 的收盘价** —— 研报在可交易日 D 开盘前生成，
那时能看到的最后一根完整日线是 D−1。

未来数据从**两个入口**渗入：
1. `_drop_incomplete_bar` 只剔"≥today 的最后一根"，回放时序列早已长到 today 之后
2. 长桥 K 线判据是"谁更新用谁"，修好第一处后它又把未来数据接了回来

**修法**：新增 `_truncate_before()`，在**所有价格来源汇合之后**统一再截一次。

### 🟠 P1-1 index 报 call/put 张数比会误导
`undertow/cli.py` `_flow_facts`

白银 index 写「call 是 put 的 2.2 倍」看着像看涨，可那批 call（69C/67C，
纯净度 0.95+）全是**卖方压制** —— 卖上方 call 是看跌动作。当天白银 **−4.38%**。

**修法**：按买卖方分侧（看跌侧＝买 put＋卖 call）。用 flow 的 `bias` 字段
而非匹配 judgment 文案 —— 那有「买方保护」「买方轻微保护」
「买方保护(新建·主动方未知)」多种变体，字符串匹配必漏。
白银改后显示「看跌侧 2.4 倍」，与实际一致。

### 🟠 P1-2 短到期低 Δ 被标成「彩票」，判断反了
`undertow/report/html.py`

我把黄金 413P/408P 标成「彩票·几乎没方向暴露」。**反了**：
长到期+低 Δ 才是尾部险；**短到期+低 Δ+大量新开仓 = 只有立刻大跌才值钱的
高杠杆定向押注，方向信息最强**。现在标 `⚠️赌急跌 / ⚠️赌急涨`。

### 🟠 P1-3 研报按生成日期命名
`undertow/cli.py`

周六生成的报告装着描述 8/27 的数据、可交易日是 8/28，却叫 `gold_2026-08-29.html`。
**研报的名字必须回答「这份东西哪天能用」**。改用快照日期（可交易日），
数据非当日时明确提示。

### 🟠 P1-4 launchd 密集日历时点不投递
`scripts/session_hooks.sh` + plist

15 个 StartCalendarInterval 时点，当天该响 12 枪，**几乎一枪没投**。
逐一排除脚本/环境/plist未重载/机器睡眠后，守到 23:12 那一枪确认未投递。
同机器上 StartInterval 的任务跑了 353/587 次零故障。

**修法**：改 `StartInterval=300` 轮询，脚本自己按真 ET 判窗口
（顺带解决夏令时，不用再挂两套时点）+ 每次唤醒留心跳。

---

## 二、新增能力

| 模块 | 作用 |
|---|---|
| `analyze/position_alert.py` | 持仓 × 信号冲突告警。**黄金的信号能惊动白银的持仓**（相关 0.89）。只上终端+通知+`data/account/`，绝不进 HTML |
| `analyze/family.py` | 同族一致性：金银 0.89、QQQ/TQQQ 0.99 结论不一致时并排摆出来。同向时完全沉默 |
| `analyze/indicators.py` | 六组指标 label（index 小标签 + 品种研报详解栏目），每组写明「何时可信」 |
| `analyze/strength.py` | 按验证结果分权的综合评分，与现行分并列显示 |
| `analyze/squeeze.py` | 波动压缩观察项。只说"可能要变天"，从不说"往哪边变" |
| `report --as-of` | 历史回放，产物写 `data/reports/replay/` 不污染归档 |
| `scripts/wait_settlement.sh` | 盯 OCC 结算落地，一到就抓+出研报+通知 |

---

## 三、回测发现（含被推翻的结论）

**138 个「品种×可交易日」样本，日聚类 bootstrap，前瞻 1 日，局部去趋势。**

> ⚠️ **以下结论已于同日被 codex review 推翻一半，见文末「撤回」一节。**

### 分层前 vs 分层后 —— 用户的方法论改进救活了两个指标

| 指标 | 混在一起 | 大波动日 | 横盘日 |
|---|---|---|---|
| 💰 增仓 | 64.3% | 63.9% [50.0,77.4] | **64.5% [53.9,74.6] ✅** |
| 🌊 波动 | **52.1%（我判它无效）** | **72.7% [55.2,88.6] ✅** | **41.3%（反向）** |
| 📈 价格 | 60.2%（贴边） | **73.5% [57.1,87.9] ✅** | 53.7% |

**不分层的回测会把「只在特定行情下有效」误判成「无效」。**
波动率面混在一起测只有 52.1%，等于把 72.7% 和 41.3% 两个方向相反的东西平均掉了。

### 被推翻的结论

1. **强度没有额外价值**（与我和用户的共同预期相反）
   - 高强度组 vs 低强度组：+2.5pp / 0.0pp / −1.2pp
   - 「按强度加权」减「只用方向」：中位 +3.1pp，95% 区间 **[−3.7pp, +9.7pp] 跨 0**
   - 压力比 ≥20× 的 11 个饱和样本命中 **6/11 = 55%**，反低于全体 64.3%
   - 大波动日内高强度组 55.6% vs 低强度组 72.2%（**−16.7pp**）
   - → `USE_STRENGTH = False`，只用方向 × 组权重

2. **「事前预判大行情」我一度读反了方向**
   先测「IV 高 → 次日大波动」得到高 IV 组 23% / 低 IV 组 40%，
   记成"预判失败"。用户提醒三角形形态后才看清：**同一份数字，
   不是 IV 高预示爆发，是 IV 被压到低位才预示爆发**。

   按压缩重测（基准大波动率 30%）：
   - ATM IV 低位：最压缩 1/3 → **45%**，最舒张 → 25%，**+20pp**
   - ATR5/ATR60 收缩：**45%** vs 27%，**+18pp**
   - 10/60 日区间收敛（三角形本身）：**−9pp ❌ 不成立，不纳入**
   - 三重同时：44% vs 29%，+15pp（n=18）
   - ⚠️ 95% 区间跨 0，样本不足 → 只做观察标记，不进综合分

3. **波动不聚集**：前日波动大 → 今日大波动 23%；前日小 → 45%。**负相关**，
   不能用前日波动择时。

### 8/28 三套评分对比（一天数据，不作证据）

| | 命中 |
|---|---|
| 现行综合 | 1/8 |
| 按验证权重 | 4/8 |
| 仅近端 | 5/8 |

---

## 四、已知缺陷（写在页面上，没有藏）

1. **各组权重固定，不随读数强弱变化**：「增仓 53.5×」和「增仓 1.4×」
   在综合投票里同样只值 0.8 票。强度只在 label 和指标表里看得到。
2. **struct 与 flow 并非真正非同源**：同一份快照，一个看存量一个看增量。
   代码注释里仍写着「六组互不同源」，表述需要收紧。
3. **回测样本只覆盖 2026-06-25~08-28 两个月**，主力是金银油（各 38 个）+ QQQ（18 个），
   其余品种各只有 2 个。一种市场环境，结论谈不上牢固。
4. **COT / 宏观两层未回测**：周频月频数据现在拉到的是最新值而非当时的值，
   拿它回测就是 lookahead。宁可不测。
5. **`squeeze` 的 IV_TIGHT=0.35 / ATR_TIGHT=0.85 是拍的**，未校准。


---

## 五、codex review 后的撤回与修正（2026-08-29 晚）

codex 推翻了本轮最核心的两个结论，**都成立**。

### ❌ 撤回：「增仓已验证有效」

我说增仓层 64.3%、日聚类 95% 区间 [54.5%, 73.1%]、下界 >50%，是项目第一个
通过门槛的指标。**错在计数单位。**

一旦承认跨品种同日高度相关（金银 0.89、QQQ/TQQQ 0.99），有效独立单位就是
**日期聚类**而不是品种日。全样本只有 **35 个日期聚类 < 50**，不满足项目
自己定的 n≥50。继续拿 138 个品种日去凑，等于一边承认相关、一边按独立计数。

**→ Flow / Vol / Price 三层目前一个都没验证通过，全是探索性观察。**

### ❌ 撤回：「按验证结果分权」

`GROUP_W` 是看着同一批回测的命中结果定的，属于结果驱动的参数选择，
即便没做网格搜索也是拟合。

**实证**：把 GROUP_W 从 `{flow1.0, struct0.6, vol0.6, price0.8}` 改成
`{flow1.0, struct0.5, vol0.3, price0.7}` 之后，"强度 vs 方向"那一节的结论
就从 **+3.1pp（区间跨 0）变成 +12.6pp（区间不跨 0）** —— 同一批数据，
换个权重就换个结论。

改用等权后回到 **+2.3pp，区间 [−5.8, +10.6] 跨 0**，`USE_STRENGTH=False`
的判断得以保住。UI 上「按验证权重」改名「实验分」，常量标 `EXPERIMENTAL_UNVALIDATED`。

### 🔴 数据损坏：回放污染正式台账

我在提交说明里写「回放不污染台账」，**实测跑一次 `--as-of` 就改写了**
`outlook_scores.json` 与 5 个 `resonance/*.json`。四个持久化点全部加了
replay 守卫。

### 🔴 回放读的是最新快照

`latest_two()` 永远取最新两份 —— 回放 8/20 会拿 8/27+8/28 冒充当天。
（给用户看的 8/28 回放碰巧是对的，因为 8/28 就是最新。）

### 🔴 squeeze 在生产里永远触发不了

`VolRegime` 根本没有 `history` 字段，`_ivh` 恒为空。而且文案里的「45%」
是回测中各**单项**最低三分之一的数字，不是这个 AND 规则的胜率 ——
那个精确布尔规则从未被单独回测过。改用 `VolRegime.iv_pct`，删掉胜率断言。

### 🔴 仓库里的 plist 根本没改

我只改了本机 `~/Library` 副本，提交说明却宣称改完了。**换台机器装上就是坏的**，
而测试因为只读本机副本、不存在就 skip，全程绿灯。

### 其余

- `_spread_bias` 会把铁鹰误判成看跌、给日历价差任意定向 → 只认标准垂直价差，其余返回「不知道」
- 回放的 COT/宏观/波动率指数/事件/4H 仍是当前值，无历史 vintage → 终端明示哪四层干净、哪两层不可信
- 回测脚本的特征分位按全区间排序，含未来信息 → 标注为事后描述，非盘前可复现的预测回测
- 「局部去趋势」实为全样本品种均值，含未来信息 → 改正表述
- 账户读取失败静默返回 → 现在会说「未能执行检查」，不再让人以为「没有冲突」

### 当前诚实的状态

**没有任何一层指标通过了本项目的验证门槛。**
增仓层最有希望（64.3%、区间下界 54.5%），但要等到至少 50 个独立日期聚类
—— 按每天约 1 个聚类算，还需要约 15 个交易日。


---

## 六、第二批更新（同日晚，用户逐条追问引出）

这一批全部由用户的具体质疑驱动，每条都能追溯到一个「你这里不对」。

### 6.1 白银为什么还是「偏多」—— 展示层在掩盖构成

白银 8/28 的真实构成：看涨 **3 票全是 Macro**（+3.1），看跌 1 票是 Flow（−0.8），
Gamma 内部抵消（+0.1）。近端 **−0.7**，差 **0.1** 没够到 −0.8 门槛 → 显示「中性」
→ 两层不冲突 → 连分层都不显示，只剩一个「偏多」。**那天白银 −4.38%。**

改动（**只动展示，不动判定** —— 按结果调 Flow 权重就是拟合）：
- 每张卡都写「近端 X(分) ｜ 中期 Y(分)」，**两层同向也分开写**
- 带上分数：门槛是拍的，−0.7 和 0.0 都叫「中性」却是两回事
- 贴着门槛仍判中性的单独标注：「近端实为偏空倾向，仅差 0.1 未过门槛」
- 底部加图例：两层各由谁投票、频率、对谁重要、门槛表

### 6.2 移除「综合」label

用户：「你的综合偏多就算了吧，这个 label 干嘛用呢？有近端和中期了」——
综合正是把两层压成一个字的那个东西。已从索引页移除（内部仍供 verdict/策略层用）。

### 6.3 近月远月是否分开 —— 半对半错

**已分开**：逐腿主键 `(到期, 行权价, C/P)`；IV 偏斜斜率每个到期自己拟合；
相对化基准按到期分组；只看 ≤45 天 + 现价 ±15%。

**没分开**：最终 `upside/downside_pressure` 把 45 天内所有到期**加总**。

新增 `dominant_expiry()`：按真实到期日聚合，占比 >40% 才算主力，否则明说
「无单一主力」。8/28 实测**只有黄金有单一主力（当日到期，占 68%）**，
其余七个全是分散 —— 68% 这个集中度本身就是信号。

分桶表拆成四列：买put(花钱押跌)／卖call(收钱压制)／买call／卖put。
起因：22天+ 桶合并显示「看跌 14.6×」，拆开是买 put 仅 **312 张**、卖 call **3,774 张**
—— 远月端并没有规模相当的主动买 put。

### 6.4 ATM IV 的合并口径问题（与外部同类读数比对时发现）

绝对水平很接近（我们 24.64% vs 他 24.16%），差在变化量。按到期拆：
`0-2天 +3.62pp`、`3-7天 −0.70`、`8-21天 −0.61`、`22-45天 −0.16`、`45天+ +0.11`。
我们把 0DTE 的 +3.62pp 和长期混着加权 → −0.29pp；他只看 10 月 → −1.13pp。
**事件日 0DTE 的 IV 必然飙升（时间价值归零 + gamma 爆炸），不该与 30 天期加权平均。**
→ 已记录，**未改判定**。这也可能是「🌊 波动」指标在横盘日反向（41.3%）的原因之一。

### 6.5 「他给了位置，我们能给吗」—— 能，数据一直有

我们报的 put 墙 GLD 413（≈金价 4560），42,388 张里 **40,394 张（95%）当日到期** ——
收盘就归零，是当日的 pin 不是承接区。
排除 ≤7 天重算，第一大是 **GLD 400（≈金价 4416）OI 59,563**，
那才是真正多到期分布的承接区。

新增 `persistent_walls()`，index 加一行「⏳ 7 天以上才算数（波段用）」。

### 6.6 破墙前有没有信号 —— 有模式，但回测否定

8/28 黄金：420P 平掉（IV −2.5pp）→ 413P 加 39,664（IV +1.0pp）→
408P 加 38,319（IV **+3.6pp**）。当日收 **408.89**。
写死逻辑后 `break_warning()` 触发且给出 next_line=408，看着极准。

**⚠️ 回测否定**：触发 30 次 → 跌破 1 次（3%）；未触发 108 次 → 跌破 1 次（1%）；
二项 p = 1.0000。两个原因都不是调参能救的：触发日里墙大多离现价 2~18%；
146 天里只有 2 次真跌破 put 墙（1.4%）。
**不上线，测试锁死 cli/html 不得引用。**

用户假设「破墙需要同时具备极强看跌信号」也不成立：交集 5 天只中 1 次。
但浮现出第三个变量 —— **墙离现价的距离**（失败四次都在 8~16% 外，8/28 只有 −2.3%）。
n=1，按用户意见先放着，不据此调参。

### 6.7 墙附近的三种形态（描述，不是预测）

旧版只认「保护向墙下搬家」，白银什么都输出不了。`wall_structure()` 三种都认：

| 形态 | 8/28 命中 | 当日结果 |
|---|---|---|
| 保护向墙下搬家 | 黄金、QQQ、SPY、IWM | 黄金破了，**QQQ 没破** |
| 就地加固这道墙 | 白银、TLT | 都守住 |
| 墙上方有人卖 put 托底 | 原油 | 守住 |

白银实例（当日收 60.02，正好守在墙上）：
```
🧱 就地加固这道墙
墙上方：买保护 加 3,460 张、卖 put 收权利金 加 3,095 张   IV −1.5pp
60（墙，−5.8%）：买保护 加 9,506 张   IV +1.0pp
墙下方 58（−8.9%）：买保护 加 228 张   IV +3.5pp
```

QQQ 的反例直接写进展示块的免责里 —— 形态不是预测。

### 6.8 研报板块重排

`① 期权关键点位 → ② 综合研判（与索引页同源）→ ③ 决策研判 → ④ 指标说明
→ ⑤ 大白话速读 → ⑥ 按到期拆开 → ⑦ 结构读数 …`

关键点位原来排在**第 7 位**。新增 `render_summary_card()` 直接复用 index 的渲染件 ——
此前研报的「决策研判」与 index 摆的事实完全脱节，**同一份数据两套说法**，
这本身就是「指标互相打架」的观感来源。

### 6.9 一个贯穿始终的根因：0DTE 污染

同一个问题在**三处**显形，用户逐个问出来的：
1. 0DTE 的 IV +3.62pp 污染了 ATM IV 合并读数（**未改，仅记录**）
2. 0DTE 的 83,450 张买 put 主导了增仓信号（**未改，加了分桶展示**）
3. 0DTE 的 40,394 张 OI 劫持了墙位（**已改，新增持续墙**）


---

## 七、codex 第二轮 review（同日）—— 两个 P0 都成立

### 🔴 P0-1 第三方来源标注进了公开路径，且已推送

代码注释、CHANGELOG、HTML 文案里出现了外部分析者的姓名、转述与其给出的点位，
**85 个 tracked 文件受影响**（约 80 个是 replay HTML，因 flow.py 的文案被渲染进去）。
其中一处（`flow.py:279`）是更早就有的。

处理：清理源码与文档 → 删除全部 replay 归档并用干净文案重新生成 →
新增扫描测试（禁词表用拼接构造，否则测试文件自身会命中自己）→
**用 `git filter-repo` 重写全部历史并强制推送**（`928f99d → 6c27d27`，247 个提交）。
本地保留备份分支 `backup-before-rewrite-20260830-1216`。

**我们自己算出来的数据（如 GLD 400 这道墙）保留** —— 要禁的是归属与转述，不是数据。

### 🔴 P0-2 不上线的预测器被换名上线了

`wall_structure()` 用的正是 `break_warning()` 的两个门槛做筛选，
输出「向墙下搬家 / 就地加固 / 卖 put 托底」这类结论，配「在给跌破定价」
「下一道防线」的预测措辞与红色高亮置顶。只是没直接调用那个函数，
语义与阈值并未隔离；而测试只搜字符串 `break_warning`，防不住等价实现。

**末尾写一句「不是预测」，抵不掉正文措辞与视觉分量。**

重写为纯事实：不筛选、不分类、不给目标价、不用方向色，只报三个区域的
ΔOI 与相对 IV，外加一行「墙下方与墙上的 IV 变化之差」。
测试改为 AST 行为断言（检查函数体是否**实际引用**那两个门槛，
注释里说明为何不用是允许的）。

### 🟠 P1 六条，其中一条推翻了本文档 §5 的结论

**P1-5 去趋势仍用全样本均值 —— 这是 lookahead。**
改为只用信号日之前 60 天的滚动漂移、窗口不足 20 天剔除且不补零之后：

| | 旧（全样本均值） | 新（滚动漂移） |
|---|---|---|
| 黄金 | 23/32 = 72%，p=**0.0201**，下界 54.6% | 9/12 = 75%，p=**0.1460**，下界 46.8% |
| 白银 | 20/28 = 71%，p=**0.0357** | 样本不足，整体剔除 |
| 原油 | 16/30 = 53% | 7/10 = 70%，p=0.3438 |

**→ §5「修正过度撤回」里那句「比探索性观察强，够不上已验证」也要撤回。**
样本从 32 降到 12（前 20 天没有足够前置窗口），命中率仍高但统计上说明不了任何问题。
**这是同一个结论在一天内的第三次修正**：过松 → 过紧 → 再撤。

其余五条：
- **P1-3** 破墙回测对两组各做 p=0.5 二项检验，答非所问。改 Fisher 精确检验（p=0.3888）。
  正确表述是「没有证据表明两组不同」，**不是**「两组没有差别」。
- **P1-4** 「各到期一致」把单桶也算共识 → 改三态 agree/conflict/insufficient。
- **P1-6** 差值区间跨 0 却给出「转铁鹰」建议 → 改为只作描述性分层。
- **P1-7** `persistent_walls` 只加了展示字段，**原墙仍进 Gamma 投票与策略** ——
  一道当天消失的 0DTE 墙仍能改变方向票。已写明，彻底修法（拆两个类型）尚未做。
- **P1-8/9/10** 持续墙只比 put 侧、回测静默丢样本、主力到期用 abs(ΔOI) 却称"增仓"。

### 当前诚实状态（比上午更保守）

**没有任何一层指标通过验证门槛，连「最有希望」都要打折。**
增仓层在正确去趋势后只剩 12 个黄金样本、p=0.146。
0DTE 污染在三处显形，其中**两处仍未真改**（ATM IV 合并读数、Gamma 层投票用的墙）。


---

## 八、出场口径与方向分组回测（2026-08-30）

用户要求：两个口径都试、加止盈、看墙位是否达到、看涨侧也要统计。

**⚠️ 样本只有 17 次（小时线覆盖 2026-07-21 起），且看涨仅 4 次。以下全部不构成结论。**

### 8.1 四种出场口径（标的 ETF，未计期权点差）

| 口径 | 次数 | 胜率 | 累计 | 平均 | p |
|---|---|---|---|---|---|
| A 开盘进→当日收盘出 | 17 | 59% | +4.24% | +0.25% | 0.629 |
| B 开盘进→次日收盘出 | 16 | 56% | **−3.43%** | −0.21% | 0.804 |
| C 止盈 0.5% 否则当日出 | 17 | **88%** | +4.14% | +0.24% | 0.002 |
| D 止盈 0.5% 否则持次日 | 17 | **94%** | +2.40% | +0.14% | 0.000 |

**止盈 0.5% 把胜率拉到 88~94%，但累计收益没提高（+4.14% vs +4.24%）** ——
它只是把分布变窄：小赢变多、大赢被砍掉。高胜率在这里不等于更赚钱。

**持有到次日普遍更差**（B 的累计是负的）。

### 8.2 分方向 —— 最重要的一条

| | 次数 | A 口径胜率 | A 口径累计 |
|---|---|---|---|
| **看跌** | 13 | **46%** | **−0.82%** |
| **看涨** | 4 | 100% | +5.06% |

**看跌信号在开盘→收盘这个真正可交易的时段里是亏的。**
此前 C2C 口径算出的 70% 里，混进了开盘前已经发生、抓不到的隔夜跳空。
看涨 4 次全对但样本太少（p=0.125）。

### 8.3 墙位作为日内目标：基本不可用

信号日当天价格触及对应方向的墙：**1/16 = 6%**（唯一一次是 2026-08-28 黄金）。
墙距开盘价中位数约 −6%，当日走到本就是小概率。**把墙当日内目标位，多数时候等于没设目标。**

### 8.4 期货能否吃到隔夜（用户问「币安期货可以抓到吗」）

用 GC=F 等 24 小时期货拆时间轴：

| 时段 | 次数 | 胜率 | 累计 | 平均 |
|---|---|---|---|---|
| 前日16:00 → 当日ET02（信号还没生成） | 20 | 55% | +4.63% | +0.23% |
| 当日ET02 → ET09:30（信号可读，期货可交易） | 20 | 60% | +3.26% | +0.16% |
| **当日ET09:30 → 16:00（美股时段）** | 20 | **70%** | **+10.64%** | **+0.53%** |

技术上期货确实能吃到 ET02→09:30 那段，但**那段胜率最低、收益最小**；
真正的收益在美股时段，而那段 GLD 期权就能做。
另：GC 一手 100 盎司（名义约 $45 万）、MGC 10 盎司（约 $4.5 万），
对 $440 的账户都不现实。

新增脚本：`backtest_exits.py`、`backtest_wall_targets.py`、`backtest_intraday_timing.py`。
