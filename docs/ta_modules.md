# 技术面子模块层（undertow/analyze/ta/）

2026-09-03 立。

> 每个技术指标，都是技术面大模块里的一个子模块。
> 我会把 trading view 上比较热门的技术指标复制过来，你看看觉得有用，
> 就建立成一个独立的子模块。我们不一定用它，可以先建好保存。
> —— 用户 2026-09-03

## 这一层的规矩

**默认不进研报、不进方向投票。** 先建好、能取数、有测试锁住口径，用得上时再单独接线。
任何子模块要进研报，得先过统计检验（`analyze/validation.py`）——
我们在期权层已经栽过"单信号误读资金流"的坑，技术面不重蹈。

## 周期分工（用户定义，硬约束）

| 周期 | 角色 | 用途 |
|---|---|---|
| 15m | entry | **只看开仓平仓的入场出场时机** |
| 1h | direction | 方向与波段/趋势 |
| 4h | direction | 方向与波段/趋势（用户主看） |
| 1d | direction | 方向与波段/趋势 |

**15m 的读数不得用于判断方向。** 15m 上的 MACD 金叉在日线级别可能只是噪声里
的一次抽动，拿它当方向信号等于把噪声当趋势。`read_multi()` 默认周期是
`("1h","4h","1d")`，要看 15m 必须显式传入。

## 已建子模块

### frames —— 多周期数据层

长桥 K 线周期里**没有 4h**，由 1h 按交易日分组聚合。
不能对全序列每 4 根盲分：美股 RTH 每日 7 根 1h，不是 4 的整数倍，
盲分会把前一日尾盘和次日开盘拼成一根、横跨隔夜跳空
（2026-08-27 实测 RSI6 一度算出 100）。

取不到数据抛 `KlineUnavailable`，**不返回空列表** —— 否则"没数据"会被
静默当成"没信号"。

实测四个周期都到位（2026-09-03 盘前，末根均为 9/2）：

```
 15m  200 根  2026-08-24 → 2026-09-02   [择时]
  1h  250 根  2026-07-15 → 2026-09-02   [方向]
  4h  200 根  2026-04-13 → 2026-09-02   [方向]
  1d  300 根  2025-06-25 → 2026-09-02   [方向]
```

### macd —— CM_Ult_MacD_MTF 口径

用户提供的是 ChrisMoody 2014-04-10 的脚本。移植时有**一处必须注意的口径差异**：

```pine
signal = sma(macd, signalLength)    // ← SMA，不是 EMA
```

TradingView 内置 MACD 和绝大多数教科书用的是 `EMA(macd, 9)`。
两者在拐点附近能差出一整根的方向 —— 实测 GLD 4h 柱状图
SMA 口径 −3.896 vs EMA 口径 −3.308，差 18%。

既然用户照着这个脚本看盘，本模块**默认跟它一致**（`signal_ma="sma"`），
保留 `"ema"` 选项对照。口径不一致就是对不上盘 ——
2026-08-27 技术面滞后两天那次，根子就是我们和用户 App 不同源。

另一处易错点：Pine 的 `ta.ema` 种子是 **SMA(n)** 而非首值。
用首值当种子会让前几十根偏离，早期柱状图对不上 TradingView。

**四色柱**是这个脚本比裸看金叉多出来的信息：

| 颜色 | 条件 | 含义 |
|---|---|---|
| aqua | hist>0 且走强 | 多头强化 |
| blue | hist>0 但走弱 | 多头衰减（顶部预警） |
| red | hist≤0 且走弱 | 空头强化 |
| maroon | hist≤0 但走强 | 空头衰减（底部修复） |

颜色翻转**早于零轴穿越，也早于金叉死叉**。blue 出现时价格常还在涨，
但动能已在减速。注意脚本里 `hist <= 0` 进 histB 分支，**0 归零下侧**。

## 查看入口

```
undertow ta                      # 配置里所有品种
undertow ta GLD.US SLV.US        # 指定标的
undertow ta SLV.US --frames      # 只做数据时效体检
undertow ta SLV.US --signal-ma ema   # 换成 TradingView 内置口径对照
```

## 规划中

- `analyze/smc.py` 目前仍在上层（先前已接研报），后续可平移进来
- **反向导出 Pine**：把我们自研的指标（墙位、极强信号）写成 Pine 脚本贴到
  TradingView 上看 —— 用户 2026-09-03 提出，先记，未做。
  难点在于墙位数据来自期权链，TradingView 上没有这个源，
  可能得走「离线算好 → 硬编码进脚本 → 定期刷新」的路子。

---

### supertrend / ut_bot —— 两个 ATR 追踪止损（用户 2026-09-03 问「有什么区别」）

**同一族**：都是「ATR 追踪止损 + 状态机」。差异在轨道结构和敏感度。

| | Supertrend | UT Bot |
|---|---|---|
| 源 | `hl2`（含影线） | `close` |
| ATR 倍数 | **3.0** | **1.0** |
| 轨道 | 上下**两条**棘轮轨，trend 决定显示哪条 | **一条**追踪止损线，自己在价格两侧翻转 |
| 翻转比较对象 | close vs **前一根**的对侧轨（`up1`/`dn1`） | src vs **前一根**的 stop |
| ATR 口径 | 可切 RMA / SMA(tr)，默认 RMA | 固定 RMA |
| 可选源 | — | Heikin Ashi（默认关） |

移植时最容易写错的一处：Supertrend 的翻转判定用的是 `up1`/`dn1`（**前一根**的轨），
不是当根值。写错了信号提前一根 —— 4h 上就是提前 4 小时，回测会凭空多出一截收益。

UT Bot 源码里两处值得知道：
1. `ema = ema(src, 1)` —— 周期 1 的 EMA 就是 src 本身（alpha=1）。绕这圈只为用 `crossover()`。
2. `buy = src > stop and above`，而 `above = crossover(src, stop)` 已蕴含 `src > stop`，
   **前半个条件是冗余的**，`buy ≡ above`。

#### SuperTrend STRATEGY 版（用户 2026-09-03 问「这个策略是一样的吧」）

**计算部分逐字符相同。** 从 `atr2 = sma(tr, Periods)` 到
`sellSignal = trend == -1 and trend[1] == 1`，两份是同一段代码，
同一作者（KivancOzbilgic）把 indicator 包成了 strategy。实测在 SLV 4h 上
两者的 buySignal/sellSignal 下标完全一致，我们的 `supertrend.py` 已覆盖，
**不需要新模块**。

差的是包装，而包装带来五处实盘差别：

1. **always-in-market**。`strategy.entry` 在反向信号时自动平仓反手，
   **没有空仓状态**。永远满仓，只在多空之间切。
2. **无止损、无止盈、无仓位管理**。每次翻转全仓反手。
3. **默认 0 手续费 0 滑点**。脚本的 `strategy()` 没写
   `commission_type` / `slippage`，TradingView 策略测试器就按 0 算。
   在那上面看到的净利润曲线，是把成本假设全关掉的样子。
4. **成交在次根开盘，不是信号根收盘**。信号在收盘确认，
   `strategy.entry` 默认下一根开盘成交。
5. **可调日期窗口**（`FromYear`…`ToYear`）是过拟合的经典温床 ——
   挑一段好看的时间，曲线就好看。

第 4 条不是细节，它直接改结论：

| | 信号根收盘 | 次根开盘 | 差额 |
|---|---|---|---|
| SLV 1d ST | +83.7 | +69.0 | **−14.6** |
| GLD 1h ST | +4.8 | +0.3 | −4.5 |
| GLD 1d ST | −10.3 | −14.3 | −3.9 |

六个组合里五个变差。趋势跟踪的信号出在极值附近，次根开盘往往已经跳空过去，
这一截滑点是它的天然成本。

因此建了 `ta/backtest.py`，把三条假设焊死不留可选项：
次根开盘成交、必计换手成本、always-in-market。

#### 实测（已按真实成交假设，2026-09-03 修正版）

| | 段数 | 毛% | 成本 | 净% | 同期持有 | 净−持有 |
|---|---|---|---|---|---|---|
| GLD 1h ST | 9 | +4.3 | 0.9 | +3.4 | +8.3 | −4.9 |
| GLD 1h UT | 26 | +1.9 | 2.6 | −0.7 | +6.2 | −6.9 |
| GLD 4h ST | 9 | −1.2 | 0.9 | −2.1 | −6.7 | **+4.6** |
| GLD 4h UT | 31 | −0.8 | 3.1 | −3.9 | −8.8 | **+5.0** |
| GLD 1d ST | 13 | −16.8 | 1.3 | −17.9 | +32.2 | −50.1 |
| GLD 1d UT | 37 | +42.1 | 3.6 | +36.9 | +30.2 | **+6.7** |
| SLV 1h ST | 7 | +4.7 | 0.7 | +4.0 | +12.8 | −8.8 |
| SLV 1h UT | 30 | −6.2 | 3.0 | −8.9 | +11.7 | −20.7 |
| SLV 4h ST | 7 | +24.2 | 0.7 | +23.4 | −16.3 | **+39.7** |
| SLV 4h UT | 27 | +10.3 | 2.7 | +7.3 | −14.5 | **+21.8** |
| SLV 1d ST | 8 | +73.8 | 0.8 | +72.4 | +77.4 | −5.0 |
| SLV 1d UT | 43 | +38.2 | 4.2 | +32.4 | +71.8 | −39.4 |

**跑赢同期买入持有 5/12。**

⚠️ 这张表已经**改过三次口径**，每次都翻盘：

| 口径 | 跑赢数 | 触发的变化 |
|---|---|---|
| 信号根收盘成交 | 3/12 | 初版 |
| 次根开盘成交 | 4/12 | SLV 1d ST 由赢转输、GLD 4h UT 由赢转输、GLD 1d UT 由输转赢 |
| 复利 + 同期基准 + 段落配对修正 | 5/12 | GLD 4h UT 又翻回赢 |

第三次改口径来自 2026-09-03 的 codex review（P1-2/3/4）：收益要按权益递推
而非各段相加、基准要与策略同期、末根不可成交的 flip 不得连累上一段。
**同一份数据、同一套信号，三种都合理的核算方式给出三个不同结论** ——
这比表里任何一个数字都更能说明样本量的问题。

（成本按每次翻转往返 0.10%，按 (1−c)ⁿ 递推。）


三条读得出来的东西：

1. **样本小到换个成交假设就翻盘**（12 个里翻了 3 个）。这本身就是结论：
   7~42 段撑不起任何"某某指标有效"的说法。
2. **跑赢的四个里三个在标的下跌段**（GLD 4h −7.5%、SLV 4h −13.5%）——
   趋势跟踪的价值在于**能做空**，单边上涨里必然跑输，翻转必然发生在回调处。
3. **胜率评价这类指标是错的**。它们天生低胜率（30~44%）靠厚尾大赢单，
   看总和与盈亏比才对。但看了总和，结论仍然是没有稳定优势。
4. **UT Bot 的高频是纯损耗**。SLV 1d 上它 42 段、成本 4.2%，
   Supertrend 8 段、成本 0.8%。同一段行情，切得越碎丢得越多。

⚠️ 样本 7~42 段，**不足以支持任何结论**。上表是描述性统计，不是回测结论。
统计口径由 `ta/backtest.py` 固化，改不了成交假设。

#### 它们可能真正有用的地方

不是拿来判方向，而是**当离场线用**。Supertrend 的轨本来就是移动止损：
多头时下轨在价格下方 N%，跌破即离场。这和用户给 15m 的定位
（「只是看开仓平仓的入场出场时机」）是同一件事 —— 只不过 Supertrend
把它放在了 1h/4h 上。

要接进来的话，合理的用法是：**已有仓位时给一条离场线**，而不是给一个开仓方向。
这需要单独检验，现在没做。

---

### stoch / risk —— 取自 "EMA Trend + MTF Stochastic Strategy"（用户 2026-09-03）

这份跟前面几个不是一类：**它是完整交易系统，不是单指标**。
而且明显更成熟——写了手续费（`commission_value=0.02`）、有分阶段止损、
时间止损、风险仓位。前面那些 strategy 版全是 0 成本假设。

结构是四层：EMA(38/62) 定趋势 → MTF 随机指标择时 → 分阶段 ATR 止损出场
→ 风险仓位定手数。

#### 没有整体移植的三个理由

**1. 14 个可调参数。**
`emaFast(38)` `emaSlow(62)` `len(11)` `smoothK(3)` `smoothD(3)` `upLine(80)`
`lowLine(20)` `atrLen(14)` `atrStopMult(1.5)` `breakEvenR(1.0)` `trailStartR(1.5)`
`trailATRMult(1.5)` `maxBarsInTrade(60)` `riskPercent(1.0)`。

**38 和 62 这种数字几乎肯定是网格搜索出来的**——没有人会凭直觉选 38。
移植它等于把别人在别的品种别的周期上的过拟合搬进来。

**2. 确认仪表盘是重复计数。**

```pine
bullScore = (trendUp ? 40 : 0) + (k > d ? 20 : 0) + (mtfK > mtfD ? 20 : 0) + (k > 50 ? 20 : 0)
```

100 分里有 **60 分来自同一个随机指标的三个侧面**（k>d、mtfK>mtfD、k>50）。
这跟我们在 SMC vs 期权墙上发现的问题是同一个：同源信号相互印证是假象，
它只是把一个信号数了三遍。权重 40/20/20/20 也没有依据。

**3. 一处实质 bug。**

```pine
entryPrice := close
strategy.entry("Long", strategy.long, qty=qty)
```

`entryPrice` 记的是**信号根收盘**，而 `strategy.entry` 在**次根开盘**成交。
后面 `currentR = (close - entryPrice) / entryStopDist` 的基准，
是一个从未成交过的价格。次根跳空高开时实际成本更高，而 R 用更低的
`entryPrice` 算 → 过早判定盈利 1R → 把止损"移到保本"，
而那个位置对实际成本来说是亏的。跳空越大错得越多。

我们的 `risk.Position` 强制传入实际成交价，不接受信号价。

#### 移植了什么

**`stoch.py`** —— 随机指标本身是标准指标，值得有。
MTF 分支照实现，包括 `lookahead_off` 的对齐语义（低周期只能看到已收盘的
高周期 K 线）。有一处要知道：`ta.linreg` 是拟合不是均值，**可以外推出
[0,100] 之外** —— 实测 GLD 1h 的 MTF-k 到了 −6.9。
所以脚本的 `crossover(mtfK, 50)` 作用在一个无界量上。

**`risk.py`** —— 这是那个脚本里**唯一值得单独拿出来的东西**：
与具体指标无关的通用风控结构。入场信号可以换（Supertrend、UT Bot、
我们自己的墙位），这套出场逻辑照用。

三段式止损：初始 1.5×ATR → 浮盈 1R 移保本 → 浮盈 1.5R 开始 1.5×ATR 追踪，
只收紧不放松。止损触发用最高/最低价判定，不是收盘 ——
收盘判定会漏掉盘中被扫，回测显著虚高。

风险仓位：`手数 = 权益 × 风险% ÷ 止损距离`。每笔亏损固定为权益的 1%，
止损远则手数少。比固定手数或固定比例仓位都正确，
后两者在波动大时敞口自动放大。

#### 实测：同一组入场，只换出场结构

用 Supertrend 的翻转作入场（控制变量），四种出场：

| | 无止损 | 固定1.5ATR | 纯追踪 | 三段式 |
|---|---|---|---|---|
| GLD 1h（持有 +8.3） | +3.6 | +2.4 | −1.6 | +1.3 |
| GLD 4h（持有 −6.7） | −2.0 | −4.0 | −3.4 | −6.6 |
| GLD 1d（持有 +32.2） | −17.2 | +3.4 | +12.3 | **+15.5** |
| SLV 1h（持有 +12.8） | +4.3 | +5.8 | +5.7 | +3.4 |
| SLV 4h（持有 −16.3） | +24.2 | +12.4 | **+33.1** | +31.1 |
| SLV 1d（持有 +77.4） | **+71.4** | −7.5 | −3.3 | −5.6 |
| **合计** | **+84.3** | +12.6 | +42.8 | +39.0 |
| **去掉 SLV 1d** | +12.9 | +20.0 | **+46.1** | +44.7 |

**最优方案取决于要不要那一行。** 带上 SLV 1d，无止损碾压（+84.3）；
去掉它，无止损变成最差（+12.9），纯追踪和三段式反超到 +45 左右。

原因不难理解：SLV 那段白银涨了 78.9%，无止损的趋势跟踪能吃到大部分；
一加止损就被反复扫出去，错过大波段。**止损保护了小亏，也切断了大赢**,
而趋势跟踪的收益全靠少数大赢单。

**结论：这个测试无法证明三段式止损更好。** 6 个组合、每组 6~13 段，
任何单个组合都能主导全局。这跟上一节的教训是同一件事——
样本小到换个假设、去掉一行就翻盘。

真要用它，正确的问法不是"哪种止损更好"，而是"**我这笔仓位能承受多大回撤**"。
风险仓位那部分（每笔固定 1% 风险）不需要统计检验就成立，它是账户管理不是预测。

---

### dmi / entries / exits —— 取自 "DMI/ADX Trend Dashboard v3"（用户 2026-09-03）

用户的话：「似乎策略在不同品种中收益都会不一样。」
下面这套分析就是围绕这句展开的，结论比这句话更极端。

**22 个可调参数**，比上一份还多。同样没有整体移植，但里面有三样东西值得单拿。

#### 移植了什么

**`dmi.py`** —— DMI/ADX 是标准指标。要点：±DM **互斥**，同一根 K 线两个方向都动时
只记更大的那个，这是它与"内外包线"类指标的根本区别。ADX 是 DX 的二次平滑
（rma 套 rma），滞后很重，且**只说明在单边走，不说明往哪走**。

趋势评分照实现了，但它**又是同源重复计数**：

    ADX 水平 30 ┐
    DI 差    20 ├ 75 分全部来自 DMI/ADX 家族
    ADX 斜率 25 ┘
    EMA 斜率 25 ← 唯一独立成分

比上一份的 `bullScore`（60/100 同源）更隐蔽：ADX 水平与 ADX 斜率读起来像
两件事，其实是同一条曲线的值与导数。**"四重确认"实际是两重。**
可取之处是 `activeWeight` 随开关自动归一化，比硬编码权重好。

**`entries.py`** —— 回调入场。这是脚本里最有价值的设计，
也正是我们研报「回调买·不追」那句话的程序化版本：regime 成立 → 价格跌破
快 EMA(9) 标记「回调中」→ 重新站上才进场。对比裸 DI 交叉的即刻追高。

**`exits.py`** —— 枝形吊灯止损 + ADX 衰减出场 + 冷却期。

吊灯与 `risk.py` 的三段式追踪差在**锚点**：三段式从当前价算，
吊灯从 `highest(high, 22)` 算。所以吊灯**只有创新高才上移**，
一根回调不会收紧，在有噪声的趋势里宽容得多。

ADX 衰减出场思路新颖：**与价格无关，只看趋势强度是否见顶**。
记录进场后 ADX 峰值，回落 30% 即平仓。价格还在涨但 ADX 掉了三成，
说明推力散了。代价是 ADX 本身滞后 —— 这条规则救得了拖泥带水的横盘，
救不了急转。

#### 实测：一个作者大概率没意识到的内在矛盾

四品种 × 两周期跑下来，**每个组合只有 0~5 笔信号**。拆开各层过滤器：

| 品种 周期 | ADX≥20 | DI差>10 | 两者兼备 | +EMA过滤 | 回调触发 |
|---|---|---|---|---|---|
| 黄金 4h | 70% | 65% | 55% | 45% | 4 |
| 黄金 1d | 74% | 64% | 55% | 55% | 3 |
| 白银 4h | 78% | 66% | 57% | 52% | 5 |
| 白银 1d | 61% | 59% | 42% | 37% | 3 |
| 原油 4h | 47% | 55% | 33% | 32% | **0** |
| 原油 1d | 43% | 43% | 29% | 26% | 2 |
| 美元 4h | 55% | 58% | 39% | 39% | 3 |
| 美元 1d | 51% | 49% | 34% | 34% | 3 |

regime 通过率并不低（26~55%），瓶颈在回调触发。再往下拆：

| 品种 周期 | 跌破 EMA9 次数 | 跌破当根 regime 还在 | 站回时 regime 还在 |
|---|---|---|---|
| 黄金 1d | 23 | **1** | **0** |
| 原油 4h | 14 | **0** | 0 |
| 白银 1d | 27 | 2 | 2 |
| 美元 1d | 31 | 4 | 3 |

**这就是根因**：regime 要求趋势强（ADX≥20、DI差>10），
而回调本身就会削弱这两个指标 —— 于是回调一发生 regime 几乎必然失效，
标记被 `if not longRegime: longPullback := false` 立刻清掉。

**「等回调」和「要求回调时趋势仍强」是互斥的两个要求。**
脚本把它们写在了一起。

我们加了 `grace` 参数放宽这一条（允许 regime 在回调期间断开若干根），
但改善有限：信号数 23 → 32（grace=10）。**这套策略在我们的数据窗口里
天然低频**，不是参数能救的。

#### 回到用户的观察

「策略在不同品种中收益都会不一样」——是的，但原因不是策略在某些品种更有效：

| | 回调入场 | 裸 DI 交叉 |
|---|---|---|
| 合计 | +62.4% | +9.3% |
| 最好 / 最差 | +46.5 / −1.0 | +4.6 / −0.1 |
| 为正 | 5/8 | 3/8 |

「回调入场合计 +62.4%」里，**+46.5 来自白银日线的 3 笔**。
每个品种 0~5 笔的样本，收益差异就是随机，不是品种特性。

要真正回答"哪些品种适合什么策略"，需要的是**每个品种几十笔以上的独立样本**，
而不是把八个 0~5 笔的组合摆在一起比大小。这跟前两节的教训是同一件事。


---

## 封存说明（2026-09-04）

用户：「这些都只是备用，先放着。整理好。」

这一层**全部检验完毕，没有一个可以接入**。三条主线的最终结论：

| 检验 | 结果 | 记录处 |
|---|---|---|
| 方向有效性 | 60 个组合，57 个与「一直做多」无区别；3 个「劣于」= 期望假阳性数 | `validation.py: ta_indicators_direction` |
| 作为过滤器 | 8 个检验全不显著，方向还不一致 | `validation.py: trend_as_filter` |
| 优化出场 | 能降回撤但切断厚尾；瓶颈在「不在场」而非出场 | `scripts/st_exit_rules.py` |

**方法论上留下的东西比结论更值钱**（都在 validation.py 的 caveat 里）：

- 统计必须按品种拆 —— 合并样本让「三个指标显著劣于」变成了「美元一个品种拖的」
- 跨周期不能汇总 —— +3 根在 1h 是 3 小时、在 1d 是 3 天，检验对象不一致
- 聚类校正是必须的 —— 未校正时六个指标全「显著」，校正后只剩三个
- 双尾检验要挑对尾巴 —— 原实现让所有「显著劣于」都被算成 p=1.0
- 基线不是 50% —— 样本期上涨，一直做多就有 53.9%
- 多重比较要算期望假阳性 —— 60 次检验在 α=0.05 下期望正好 3 个显著

同一份数据、同一套信号，我给出过三个不同结论，每次都以为自己对了。
过程记在 `validation.py` 的 caveat 与 `docs/codex_review_2026-09-04.md`。

⚠️ 想重新启用任何一个子模块，先读那两条 caveat。

---

## 封存后收到的脚本（只做审查，不建模块）

### SMC Institutional Clean Wave & Structure PRO（2026-09-04）

**⛔ 第 5 节的 BUY/SELL 徽章是纯事后标注，不可交易。**

```pine
ph_sig = ta.pivothigh(high, sig_sens, sig_sens)   // sig_sens 默认 10
if show_sig and not na(ph_sig)
    int idx = bar_index - sig_sens                 // ← 画在 10 根之前
    label.new(idx, high[sig_sens] + atr*0.3, "SELL", ...)
```

`ta.pivothigh(src, n, n)` 要**右侧 n 根走完**才能确认，而标签却画在 n 根之前的
位置上。GLD 日线实测：

| 标注在 | 实际能知道 | 标注处价格 | 确认时价格 | 期间已走 |
|---|---|---|---|---|
| 2026-01-29 | 2026-02-12 | 509.70 | 451.39 | **−11.4%** |
| 2025-10-20 | 2025-11-03 | 403.30 | 368.78 | −8.6% |
| 2026-03-02 | 2026-03-16 | 492.15 | 460.43 | −6.4% |

图上那个 SELL 标在 509.70 的顶部，但你能看到它时价格已经 451.39。
在 4h 上延迟是 40 小时。**这不是滞后，是事后画上去的。**

#### 其余四节的判断

| 组件 | 实现 | 判断 |
|---|---|---|
| Dynamic Trend Wave | `ta.alma(close,21,0.85,6)`，`close>=wave` 定方向 | 又一个均线类方向指标，与已证伪的同族 |
| Smart Candle Coloring | 内包线 `high<=high[1] and low>=low[1]` | 波动收缩，我们的 `analyze/squeeze.py` 已有同类概念 |
| BOS / CHoCH | pivot 确认后，**close 上/下穿该 pivot 时实时触发** | ✅ 实时，无重绘。概念我们没有，见下 |
| −2.5 SD Target | `sma(20) − 2.5×stdev(20)` | 就是布林下轨（2.5σ 而非 2σ）。叫 "Next Target" 有误导，它是统计边界不是预测目标；且金融数据厚尾，触及频率高于正态假设 |

#### 唯一值得记的概念：BOS vs CHoCH

    上穿前高时：当前趋势为空 → CHoCH（性格转变，转折）
                当前趋势为多 → BOS  （结构突破，延续）

同样是"突破前高"，**在多头里是延续、在空头里是转折**，两者的含义完全不同。
我们的 `analyze/smc.py` 只做订单块，没有区分这两种突破。

⚠️ 但按封存约定**不建模块**。真要用，得先过 validation.py 的检验，
而且要吸取教训：按品种拆、不跨周期汇总、算多重比较。

## 审查 Pine 脚本的重绘检查清单

这次的经验值得固化。看到"信号精准踩在顶底"时，按顺序查：

1. **有没有 `ta.pivothigh/pivotlow(src, L, R)` 且 R > 0？**
   → 该 pivot 要等右侧 R 根才确认。若标签画在 `bar_index - R`，就是事后标注。
2. **`request.security()` 有没有写 `lookahead=barmerge.lookahead_off`？**
   → 不写默认是 `lookahead_on`，直接泄露高周期未来值。
3. **画图坐标用的是 `bar_index` 还是 `bar_index - N`？**
   → 后者要核对那个位置的信息在当时是否已知。
4. **`strategy` 有没有写 `commission_type` / `slippage`？**
   → 不写就是 0 成本回测。
5. **`default_qty_type` 是什么？**
   → 不写默认 `strategy.fixed` / qty=1，在 100 万初始资金下敞口只有 0.04%
   （这正是同一个 Supertrend 策略报 16% 与 0.06% 的全部原因）。
